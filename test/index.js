var should = require('chai').should(),
    Brogramify = require('../index');

describe('brogramify', function() {
    it("should do something", function() {
        Brogramify.brogramify('');
    });
});